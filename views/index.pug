extends layout

block content
  h1= title

  div(class="col-md-4")
    h2 Create user
    form(action="/users/create", method="get", class="well")
      div(class="form-group")
        label(for="username")
          | Username
        input(type="text", name="username", class="form-control")
      input(type="submit" class="btn btn-default")